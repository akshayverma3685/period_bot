services:
  - type: worker
    name: ladybuddy-bot
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python main.py"
    runtime: python
    pythonVersion: 3.11.9
